
export enum Direction {
	Up,
	Down,
	Right,
	Left
}

export enum FocusDirection {
	Row,
	Column
}

export enum FocusEdgeAction {
	Flow,
	Loop,
	Lock
}

export enum FocusSkip {
	None,
	Forward,
	Backward
}
